# ═══════════════════════════════════════════
# Jbium — Python Dependencies
# ═══════════════════════════════════════════
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════

# ── Core driver ──
aiohttp>=3.9.0          # Async HTTP client (proxy IP detection, API calls)
websockets>=12.0        # WebSocket (CDP communication)
pyyaml>=6.0             # YAML config parsing

# ── GeoIP ──
geoip2>=4.7.0           # MaxMind GeoLite2 database reader

# ── Testing ──
pytest>=7.4.0           # Test framework
pytest-asyncio>=0.21.0  # Async test support
pytest-cov>=4.1.0      # Coverage reports

# ── Optional (for advanced features) ──
boto3>=1.28.0           # AWS S3 upload (scripts/upload_s3.py)
